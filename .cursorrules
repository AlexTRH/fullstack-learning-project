# Cursor Rules for Fullstack Learning Project

## Project Overview

This is a fullstack social media application built with:

- **Backend**: Node.js + NestJS + TypeScript + Prisma + PostgreSQL
- **Frontend**: Next.js 16 (App Router) + TypeScript + TanStack Query + Zustand + shadcn/ui
- **Package Manager**: pnpm
- **Architecture**: Monorepo structure

## General Principles

### Code Quality

- Always use TypeScript with strict mode enabled
- Prefer explicit types over `any` - use `unknown` if type is truly unknown
- Use meaningful variable and function names
- Keep functions small and focused (single responsibility)
- Write self-documenting code with clear naming
- Add JSDoc comments for public APIs and complex logic

### Error Handling

- Always use try-catch blocks for async operations
- Use custom AppError class for operational errors
- Never expose internal error details in production
- Return consistent error response format: `{ success: boolean, message: string }`
- Log errors with context (userId, requestId, etc.)

### Security

- Never commit `.env` files or secrets
- Always validate and sanitize user input
- Use parameterized queries (Prisma handles this)
- Implement rate limiting for auth endpoints
- Use HTTPS in production
- Validate JWT tokens on every protected route
- Hash passwords with bcrypt (minimum 10 rounds)

## Backend Rules (NestJS)

### Structure

```
backend/
├── src/
│   ├── app.module.ts       # Root module
│   ├── main.ts             # Bootstrap (NestFactory)
│   ├── application/        # Use cases (pure functions, no framework)
│   │   └── use-cases/
│   ├── domain/             # Entities + repository/service interfaces
│   │   ├── entities/
│   │   └── interfaces/
│   ├── infrastructure/     # Implementations (Prisma, bcrypt, JWT)
│   │   ├── database/       # *Repository.ts (Prisma*Repository)
│   │   ├── config/         # errors.ts, etc.
│   │   └── utils/          # BcryptPasswordHasher, JwtTokenService
│   ├── common/             # Guards, Pipes, Filters, Decorators
│   ├── prisma/             # PrismaModule, PrismaService
│   ├── auth/               # AuthModule, AuthController, AuthService, schemas
│   └── users/              # UsersModule, UsersController, UsersService, schemas
└── prisma/
    └── schema.prisma
```

### NestJS Best Practices

- Use dependency injection: inject services and repositories via constructor
- Use `@Injectable()` for services; use case functions receive dependencies from service constructors
- Use `ZodValidationPipe` with Zod schemas for DTO validation (schemas in `*/schemas/*.ts`)
- Use `JwtAuthGuard` for protected routes and `@CurrentUser()` decorator for `userId`
- Use `HttpExceptionFilter` for consistent error responses
- Use `@ApiTags()`, `@ApiOperation()`, `@ApiResponse()`, `@ApiBearerAuth()` for Swagger
- Keep `main.ts` minimal: create app, global pipes/filters, Swagger, listen

### Controllers (Presentation Layer)

- Controllers are **thin** - only handle HTTP (params, query, body) and call service/use case
- **Never** put business logic in controllers - delegate to service or use case
- Return consistent format: `{ success: true, data: T }` or throw via filter
- Use `@Controller('api/...')`, `@Get()`, `@Post()`, `@Put()`, `@Delete()` with proper decorators
- Use `@UseGuards(JwtAuthGuard)` and `@ApiBearerAuth()` for protected routes
- Use `@Body(new ZodValidationPipe(schema))` for validated body; query/params similarly
- Services inject repositories (from InfrastructureModule) and call use cases with those deps

### Use Cases (Application Layer)

- **All business logic goes in use cases**, not controllers or services
- Use cases are pure functions: `(input, dependencies) => Promise<output>`
- Services inject repositories (from Nest DI) and pass them into use cases as `dependencies`
- Use cases should be testable in isolation (mock dependencies)
- One use case = one business operation
- Use cases depend only on domain interfaces, not Nest or Prisma
- Example: `listUsersUseCase(params, { userRepository })`; service gets `userRepository` from DI and calls the use case

### Domain Layer

- **Entities**: Pure TypeScript interfaces/types representing business concepts
- **Interfaces**: Contracts for repositories and services (no implementations)
- Domain layer has **zero dependencies** on infrastructure or frameworks
- Domain entities should not have database-specific fields

### Infrastructure Layer

- **Repositories**: Implement domain repository interfaces (e.g. `PrismaUserRepository`). Created via factory `createPrismaUserRepository(prisma)`; registered in `InfrastructureModule` and injected where needed.
- **Services**: Nest services (AuthService, UsersService) orchestrate use cases; they inject repositories and optional TokenService/PasswordHasher from InfrastructureModule.
- **Prisma**: Use `PrismaService` (from `prisma/prisma.service`) injected into repository factories in InfrastructureModule.
- Domain service interfaces (TokenService, PasswordHasher) are implemented in `infrastructure/utils/` and provided by InfrastructureModule.

### Database (Prisma)

- Use `PrismaService` (extends PrismaClient) injected from PrismaModule; repositories receive it in their factory.
- Repositories live in `infrastructure/database/` (e.g. `PrismaUserRepository.ts`).
- Use transactions via `this.prisma.$transaction()` when needed.
- Use `select` to fetch only needed fields; `include` for relations (avoid N+1).
- Handle unique constraint violations (email, username) and map to AppError/409.
- Add indexes in schema for frequently queried fields; use `onDelete: Cascade` where appropriate.
- Map Prisma results to domain entities inside repositories.

### Authentication & Authorization

- JWT: access tokens (short-lived) and refresh tokens (stored in DB, longer-lived).
- Use `JwtAuthGuard` on protected routes; use `@CurrentUser()` to get `{ userId }`.
- Implement logout by invalidating refresh token (AuthService calls RefreshTokenRepository).
- Check resource ownership in use case or service before update/delete (e.g. only author can edit post).

### API Design

- Use RESTful conventions: GET (read), POST (create), PUT (update), DELETE (delete)
- Use plural nouns for resources: `/api/posts`, `/api/users`
- Use nested resources for relations: `/api/posts/:id/comments`
- Implement pagination for list endpoints: `?page=1&limit=20`
- Use query parameters for filtering and sorting
- Return appropriate HTTP status codes:
  - 200: Success
  - 201: Created
  - 400: Bad Request (validation errors)
  - 401: Unauthorized (missing/invalid token)
  - 403: Forbidden (no permission)
  - 404: Not Found
  - 409: Conflict (duplicate resource)
  - 500: Internal Server Error

## Frontend Rules

### Structure

```
frontend/
├── app/                # Next.js App Router pages
├── components/         # React components
│   └── ui/            # shadcn/ui components
├── lib/               # Utilities, API client, query client
├── stores/            # Zustand stores
├── hooks/             # Custom React hooks
└── types/              # TypeScript types
```

### Next.js Best Practices

- Use App Router (not Pages Router)
- Use Server Components by default, mark Client Components with `"use client"`
- Use `async` Server Components for data fetching
- Run independent fetches in parallel: use `Promise.all()` for multiple calls; in RSC use composition so sections don't wait on each other (e.g. separate async components for header/sidebar that each fetch their own data)
- Defer `await` to the branch that needs the result — early-return paths should not wait on unused data
- Use Suspense boundaries so layout can render while data loads in child components
- Use `React.cache()` for per-request deduplication of auth, DB, or other async work in Server Components; pass primitive or stable object args so cache hits work
- Pass minimal data into client components; avoid passing the same large object to multiple clients (duplicate serialization)
- Implement ISR (Incremental Static Regeneration) where appropriate
- Use `revalidate` for ISR: `export const revalidate = 60`
- Use `next/image` for images, `next/link` for navigation
- Never use `useEffect` for data fetching - use Server Components or TanStack Query
- Prefer direct imports for heavy libraries (e.g. lucide-react, @mui/material, date-fns) or set `optimizePackageImports` in next.config
- Use `next/dynamic` for heavy components not needed on first paint (editors, heavy modals); use `ssr: false` for client-only components
- Defer non-critical third-party scripts (analytics, etc.) until after hydration (e.g. dynamic import in useEffect or on interaction)

### React Patterns

- Use functional components with hooks
- Extract reusable logic into custom hooks
- Use `useMemo` and `useCallback` sparingly (only when needed)
- **Keep components and page files small**: if a single file grows beyond ~100–150 lines or mixes data fetching, types, helpers and a lot of JSX, split it:
  - **Pages** (`app/**/page.tsx`): only routing, auth guard, `useQuery`/`useMutation`, and composition of feature components; no large inline JSX or business types.
  - **Feature logic**: put API types and fetch functions in `features/<feature>/api.ts` or `types.ts`, helpers in `utils.ts`, UI in `features/<feature>/components/`.
  - **One component, one responsibility**: loading/error/success states can be separate small components (e.g. `*Loading`, `*Error`) composed by the page.
- Use composition over inheritance
- Prefer controlled components for forms
- Derive values during render instead of storing in state and syncing in `useEffect` (e.g. `fullName = firstName + ' ' + lastName`)
- Use lazy state init when initial state is expensive: `useState(() => expensive())`
- Use `startTransition` / `useTransition` for non-urgent UI updates instead of manual loading flags where it fits
- For conditionals that may be 0 or NaN use explicit ternary (`count > 0 ? <Badge /> : null`) not `count && <Badge />`
- Hoist static JSX and constants outside the component when they don't depend on props/state

### State Management

- **Zustand**: For global client state (auth, UI state)
- **TanStack Query**: For server state (API data, caching)
- **React State**: For local component state
- Never duplicate server state in Zustand - use TanStack Query cache

### Data Fetching

- Use TanStack Query for all API calls
- Create custom hooks: `usePosts()`, `useUser(id)`, etc.
- Use `useQuery` for GET requests
- Use `useMutation` for POST/PUT/DELETE
- Implement optimistic updates where appropriate
- Handle loading and error states properly
- Use `staleTime` and `cacheTime` appropriately

### Forms

- Use React Hook Form for form management
- Use Zod for form validation (shared with backend)
- Use `@hookform/resolvers/zod` for integration
- Show validation errors inline
- Disable submit button during submission

### API Client

- Use Axios instance from `lib/api.ts`
- Add request interceptors for auth tokens
- Add response interceptors for error handling
- Use TypeScript types for request/response
- Handle 401 errors (redirect to login)

### Styling & design tokens (single source of truth)

- **Colors and theme**: All palette and radius live in `frontend/app/globals.css` (CSS variables in `:root` and `.dark`). Use Tailwind classes that reference them: `bg-primary`, `text-muted-foreground`, `border-border`, `rounded-lg`, etc. Do not use arbitrary colors (e.g. `bg-[#fff]`) in components.
- **Layout tokens**: Use `p-page`, `md:p-page-md`, `max-w-content`, `max-w-content-narrow` from `globals.css` + `tailwind.config.ts` (same values as `p-6`, `md:p-10`, `max-w-md`, `max-w-sm`). Use these tokens for page padding and content width so changes live in one place; keep token values equal to Tailwind defaults so the UI look is unchanged.
- Use shadcn/ui components (don't write custom UI from scratch)
- Use `cn()` utility for conditional classes
- Follow mobile-first responsive design

### TypeScript

- Define types in `types/` directory
- Share types between frontend/backend when possible
- Use `interface` for object shapes, `type` for unions/intersections
- Never use `any` - use `unknown` and type guards
- Use discriminated unions for state management

## Database Schema Rules

### Prisma Schema

- Use `@id @default(uuid())` for primary keys
- Use `@unique` for unique fields (email, username)
- Use `@default(now())` for timestamps
- Use `@updatedAt` for auto-updated timestamps
- Use enums for fixed sets of values
- Add `@@index` for frequently queried fields
- Use `onDelete: Cascade` for dependent records
- Use `@@map("table_name")` for custom table names

### Migrations

- Always use `prisma migrate dev` for schema changes
- Never edit migration files manually
- Always test migrations on development first
- Use `prisma db push` only for prototyping

## Git & Commits

### Commit Messages

- Use conventional commits format:
  - `feat:` for new features
  - `fix:` for bug fixes
  - `refactor:` for code refactoring
  - `docs:` for documentation
  - `style:` for formatting
  - `test:` for tests
- Be descriptive but concise
- Example: `feat(auth): add refresh token endpoint`

### Branching

- Use `main` for production-ready code
- Create feature branches: `feature/user-profile`
- Create fix branches: `fix/auth-token-expiry`

## Code Review Checklist

Before submitting code:

- [ ] TypeScript compiles without errors
- [ ] No `any` types (unless absolutely necessary)
- [ ] All API endpoints have validation
- [ ] Error handling is implemented
- [ ] Database queries are optimized (no N+1)
- [ ] Authentication is checked on protected routes
- [ ] Environment variables are not hardcoded
- [ ] Code is formatted with Prettier
- [ ] No console.logs in production code (use proper logging)
- [ ] No unnecessary async waterfalls (use Promise.all / parallel RSC where applicable)
- [ ] Heavy UI or third-party libs use dynamic import / next/dynamic where appropriate

## Performance

### Backend

- Use database indexes for frequently queried fields
- Implement pagination for list endpoints
- Use `select` to fetch only needed fields
- Cache expensive operations (Redis if needed)
- Use connection pooling (Prisma handles this)

### Frontend

- Use Next.js Image component for images
- Implement code splitting with dynamic imports
- Use ISR for static content that updates occasionally
- Optimize bundle size (check with `next build --analyze`)
- Use React.memo for expensive components (sparingly)

## Testing (Future)

- Write unit tests for utilities and helpers
- Write integration tests for API endpoints
- Write E2E tests for critical user flows
- Aim for >70% code coverage

## Documentation

- Document complex business logic
- Add JSDoc comments for public APIs
- Keep README.md updated
- Document environment variables in .env.example

## When Writing Code

1. **Think before coding**: Understand the requirement fully
2. **Follow existing patterns**: Match the style of existing code
3. **Keep it simple**: Don't over-engineer
4. **Test manually**: Verify the code works before committing
5. **Consider edge cases**: Handle errors and edge cases
6. **Be consistent**: Follow the patterns established in the codebase

## Common Patterns

### Backend Clean Architecture Patterns (NestJS)

**Controller (thin, delegates to service):**

```typescript
// users/users.controller.ts
@Controller('api/users')
export class UsersController {
  constructor(@Inject(UsersService) private readonly usersService: UsersService) {}

  @Get('me')
  @UseGuards(JwtAuthGuard)
  @ApiBearerAuth()
  async getMe(@CurrentUser() user: { userId: string }) {
    const data = await this.usersService.getMe(user.userId);
    return { success: true, data };
  }
}
```

**Service (injects repositories, calls use cases):**

```typescript
// users/users.service.ts — injects UserRepository, calls getMeUseCase({ userId }, { userRepository })
const user = await getMeUseCase({ userId }, { userRepository: this.userRepository });
```

**Use Case (application layer, pure function):**

```typescript
// application/use-cases/users/GetMeUseCase.ts
export async function getMeUseCase(
  input: { userId: string },
  dependencies: { userRepository: UserRepository }
) {
  const user = await dependencies.userRepository.findByIdWithCounts(input.userId);
  if (!user) throw new AppError('User not found', 404);
  return user;
}
```

**Repository (infrastructure, PrismaService):**

```typescript
// infrastructure/database/PrismaUserRepository.ts
export function createPrismaUserRepository(prisma: PrismaService): UserRepository {
  return {
    async findById(id: string): Promise<User | null> {
      const user = await prisma.user.findUnique({ where: { id } });
      return user ? mapToDomainUser(user) : null;
    },
  };
}
```

### Frontend Query Hook Pattern

```typescript
export const usePosts = () => {
  return useQuery({
    queryKey: ['posts'],
    queryFn: () => api.get('/api/posts').then(res => res.data.data),
  });
};
```

### Frontend Mutation Pattern

```typescript
export const useCreatePost = () => {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data) => api.post('/api/posts', data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['posts'] });
    },
  });
};
```

## Never Do

- ❌ Don't use `any` type
- ❌ Don't commit `.env` files
- ❌ Don't expose sensitive data in API responses
- ❌ Don't use `eval()` or `Function()` constructor
- ❌ Don't store passwords in plain text
- ❌ Don't trust client-side validation alone
- ❌ Don't use `console.log` in production
- ❌ Don't ignore TypeScript errors
- ❌ Don't skip error handling
- ❌ Don't create circular dependencies
- ❌ **Don't put business logic in controllers** - use use cases
- ❌ **Don't import infrastructure or Nest in use cases** - use dependency injection (repositories passed in)
- ❌ **Don't put domain logic in infrastructure** - keep it in domain/application
- ❌ **Don't create dependencies from domain to infrastructure** - dependencies flow inward
- ❌ **Don't await independent async operations sequentially** - use Promise.all() or parallel Server Components
- ❌ **Don't use barrel imports for heavy libraries** (lucide-react, @mui/material, etc.) without optimizePackageImports or direct imports
- ❌ **Don't let page or component files grow large** (~100+ lines with mixed types, fetch, and JSX) — split into `features/<name>/api`, `utils`, and small UI components first
